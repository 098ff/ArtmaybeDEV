@startuml
title Favorites Flow: GET /api/me/favorites/
actor Client
participant "«Node.js»\nserver" as Server
participant "«router»\nfavorites" as Router
participant "«controller»\nfavorites" as Ctrl
participant "«model»\nFavorite" as FavoriteModel
database "«MongoDB»\nDatabase" as DB

== Api/me/Favorites ==
Client -> Server : GET /api/me/favorites/
activate Server
Server -> Router : router.get('/me/favorites/', getFavorites)
activate Router
Router -> Ctrl : getFavorites(req, res)
activate Ctrl
Ctrl -> DB : Favorite.find({ user }).populate('company')
activate DB
DB --> Ctrl : favorites[]
deactivate DB
Ctrl --> Client : 200 OK { count, data }
deactivate Ctrl
deactivate Router
deactivate Server
== Api/me/Favorites ==
@enduml