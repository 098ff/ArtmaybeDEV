@startuml
title Bookings Flow: GET /api/bookings
actor Client
participant "«Node.js»\nserver" as Server
participant "«router»\nbookings" as Router
participant "«controller»\nbookings" as Ctrl
participant "«model»\nBooking" as BookingModel
database "«MongoDB»\nDatabase" as DB

== Api/bookings ==
Client -> Server : GET /api/bookings
activate Server
Server -> Router : router.get('/bookings', getBookings)
activate Router
Router -> Ctrl : getBookings(req, res)
activate Ctrl
Ctrl -> DB : Booking.find(filter by user/admin)
activate DB
DB --> Ctrl : bookings[]
deactivate DB
Ctrl --> Client : 200 OK { count, data }
deactivate Ctrl
deactivate Router
deactivate Server
== Api/bookings ==
@enduml