import {configureStore} from '@reduxjs/toolkit'
import authReducer from '../features/auth/authSlice' // Import for use Reducer

export const store = configureStore({
    reducer: {
        // TODO : Implement calling Reducer here
    },
});